% external, nmap, port scan
; From https://github.com/sealldeveloper/navi-cheats/
; Tool: https://github.com/nmap/nmap

# Typical list port scan
nmap -sS -p- -A -oA nmap-fullPort -iL <listfile>

$ listfile: ls -p . | grep -v /

# Typical individual port scan
nmap -sS -p- -A -oA nmap-fullPort <ip-or-range>

% htb, nmap, port scan, hack the box, hackthebox
# HackTheBox Port Scan with verbose and T5, faster scan
sudo nmap -p- -A -T5 -v <htb-ip>

$ htb-ip: if [[ -f "ips.txt" ]]; then cat ips.txt; fi