% hashcat, kerberoasting
; From https://github.com/sealldeveloper/navi-cheats/

# crack hash using hashcat
hashcat -m <hashcat_mode> <hash_or_hashfile> <seclistdir>/<seclistlist>

$ seclistdir: (test -d "$HOME/Documents/Wordlists/SecLists/Passwords/Leaked-Databases" && echo "$HOME/Documents/Wordlists/SecLists/Passwords/Leaked-Databases") || (test -d "/usr/share/seclists/Passwords/Leaked-Databases" && echo "/usr/share/seclists/Passwords/Leaked-Databases") || echo "."
$ seclistlist: ls "<seclistdir>"
$ hash_or_hashfile: ls
$ hashcat_mode: hashcat -h 2>/dev/null | grep -E "^\s*(\d+)\s*\|\s*([^|]+?)\s*\|\s*(.+?)\s*$" | awk -F'|' 'NF==3' | sed 's/[[:space:]]\{1,\}/ /g' | sort --- --header "Mode # | Hash Desc. | Hash Type" --delimiter '\|' --column 1 --map 'awk -F"|" "{print \$1}"'