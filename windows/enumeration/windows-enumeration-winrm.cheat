% sealldev, windows, winrm, win-rm
@ sealldev, windows-variables
; From https://github.com/sealldeveloper/navi-cheats/

# evil-winrm - Connect to WinRM with evil-winrm with creds
evil-winrm -i <host_address> -u <username> -p <password>

# evil-winrm - Connect to WinRM with evil-winrm with NTLM hash
evil-winrm -i <host_address> -u <username> -H <hash>