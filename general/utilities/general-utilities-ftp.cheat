% sealldev, ftp
@ sealldev, windows-variables
; From https://github.com/sealldeveloper/navi-cheats/

# Connect anonymously to ftp
ftp anonymous@<ip>

# Connect as a user to ftp
ftp <username>@<ip>

# [INSIDE FTP] Upload a file
put <filename>

# Download a directory of FTP from wget
wget -r ftp://<ip>/<dir>