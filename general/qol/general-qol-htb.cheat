% htb, qol, hosts

# Add entry to hosts file
sudo echo "<ip> <hostname>" >> /etc/hosts

$ hostname: echo $(basename $(pwd)) | tr '[:upper:]' '[:lower:]' | sed 's/$/.htb\n/'
$ ip: if [[ -f "ips.txt" ]]; then cat ips.txt; fi --- --header "Enter the HTB Machine IP"